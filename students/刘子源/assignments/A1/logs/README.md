# Experiment logs

This directory contains public, sanitized evidence for tokenizer, training, sweep, ablation, OWT, and generation experiments. Model checkpoints and datasets are intentionally omitted. `train.jsonl` files contain step, wall-clock time, train loss, learning rate, and periodic validation loss; `summary.json` contains final metrics and public configuration fields.
